Please apply the following three specific fixes:

## 1. 긍/부정 종목 카드 — 테두리 완전 제거

- Remove ALL colored borders from 상승/호재 and 하락/악재 stock cards
- No border-l, no border outline in green or red anywhere on these cards
- Card style should be: `bg-slate-800 rounded-xl shadow-md` only
- Distinguish the two columns by section background only:
  - 상승/호재 section: subtle `bg-emerald-950/20` background on the column container (very faint, not on individual cards)
  - 하락/악재 section: subtle `bg-red-950/20` background on the column container
- Individual cards themselves: neutral `bg-slate-800` with no border

---

## 2. Datepicker 캘린더 가로 폭 정렬

- The calendar popup width must exactly match the trigger button width
- Set `width` of the calendar dropdown to equal the trigger button using a shared ref:
```js
  const btnRef = useRef(null);
  // apply calendarStyle = { width: btnRef.current?.offsetWidth }
```
- Alternatively: wrap both trigger and dropdown in a `relative` container with a fixed width (e.g. `w-52`), and set dropdown to `w-full`
- Align: `right: 0` on the dropdown so right edges are flush with the button
- No horizontal overflow — calendar grid must fit inside the dropdown box
- If calendar grid is wider than the button, reduce cell size (`text-xs`, tighter padding `p-1`) to fit

---

## 3. 섹터별 영향 분석 — 파이 차트 색상 개선

Apply distinct, vivid-but-tasteful colors per sector, consistent across both charts:

| 섹터 | Color |
|------|-------|
| 반도체 | #60a5fa (blue-400) |
| 2차전지 | #34d399 (emerald-400) |
| 바이오/헬스케어 | #f472b6 (pink-400) |
| 금융 | #fbbf24 (amber-400) |
| 에너지 | #fb923c (orange-400) |
| 플랫폼/IT | #a78bfa (violet-400) |

**긍정 파이차트:**
- Slices: full opacity (opacity: 1)
- Chart container: add a very subtle `bg-emerald-950/20` card background with a `border-t-2 border-emerald-500/40` top accent

**부정 파이차트:**
- Slices: same sector colors but slightly desaturated (opacity: 0.75) to visually distinguish from 긍정 chart
- Chart container: add a very subtle `bg-red-950/20` card background with a `border-t-2 border-red-500/40` top accent

**차트 타이틀:**
- 긍정 언급 섹터 분포: title color `text-emerald-400`
- 부정 언급 섹터 분포: title color `text-red-400`

**공통:**
- Pie label text: white, `fontSize: 12`, show percentage only (no full sector name on slice — name goes in legend)
- Legend: show sector name + color dot, placed below each chart
- innerRadius: 50, outerRadius: 90 (donut style, same as current)